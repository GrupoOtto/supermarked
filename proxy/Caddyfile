0.0.0.0:{$PORT} 
gzip
proxy /employees {$EMPLOYEES_API}:{$EMPLOYEES_PORT} {
    without /employees
}
proxy /products {$PRODUCTS_API}:{$PRODUCTS_PORT} {
    without /products
}
proxy /coupons {$COUPONS_API}:{$COUPONS_PORT} {
    without /coupons
}